import styled from "styled-components";
import "./../../index.css";

export const TagItem = styled.div`
  /* Auto Layout */

  display: flex;
  flex-direction: row;
  justify-content: center;
  align-items: center;
  padding: 3px 10px;

  position: static;
  width: 62px;

  /* Bluescale / Bright Purple Blue */

  background: #f3f3ff;
  border-radius: 10px;
  font-family: "Poppins", sans-serif;
  font-style: normal;
  font-weight: 500;
  font-size: 12px;
  line-height: 14px;
  /* identical to box height, or 117% */

  /* Bluescale / Purple blue */

  color: #5a5a89;
`;

export const TagContainer = styled.div``;
